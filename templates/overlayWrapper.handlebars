		<div class="{{CSS_CLASSES.overlay}}">
                                                            <div style="visibility:hidden">{{id}}</div>
                                                            <select multiple="multiple" id="filterSelect">
                                                            {{#each nodeFilters}} <option value={{@key}} selected="selected"> {{@key}} </option> {{/each}}
                                                            {{#each tileFilters}} <option value={{@key}} selected="selected"> {{@key}} </option> {{/each}}
                                                            </select>
			<span class="{{CSS_CLASSES.close}}">close</span>
			<div class="{{CSS_CLASSES.overlayContent}}">
			</div>
		</div>